name: Auto Transcribe & Commit Subtitles

on:
  push:
    paths:
      - "examples/**"

permissions:
  contents: write

jobs:
  transcribe-and-commit:
    runs-on: ubuntu-latest

    env:
      # allow "import autosub" from src/autosub
      PYTHONPATH: src

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Transcribe changed files (robust + debug)
        env:
          MODEL: small
        run: |
          set -euo pipefail

          echo "GIT INFO: BEFORE=${{ github.event.before }}, AFTER=${{ github.sha }}"
          echo "Raw git diff (names only):"
          git --no-pager diff --name-only ${{ github.event.before }} ${{ github.sha }} || true
          echo "----"

          CHANGED_RAW=$(git --no-pager diff --name-only ${{ github.event.before }} ${{ github.sha }} || true)
          printf "%s\n" "$CHANGED_RAW"
          echo "----"

          CHANGED=$(printf "%s\n" "$CHANGED_RAW" | grep '^examples/' || true)

          if [ -z "$CHANGED" ]; then
            echo "No example files in diff. Falling back to scanning examples/ for media files."
            mapfile -t FILES < <(find examples -maxdepth 2 -type f \( -iname "*.mp4" -o -iname "*.mkv" -o -iname "*.wav" -o -iname "*.mp3" -o -iname "*.mov" \))
          else
            mapfile -t FILES < <(printf "%s\n" "$CHANGED")
          fi

          if [ ${#FILES[@]} -eq 0 ]; then
            echo "No media files to process. Exiting."
            exit 0
          fi

          echo "Files to process (${#FILES[@]}):"
          for f in "${FILES[@]}"; do
            echo " - '$f'"
          done

          FAIL_COUNT=0
          for f in "${FILES[@]}"; do
            if [ ! -f "$f" ]; then
              echo "Skipping (not a regular file): $f"
              continue
            fi
            echo "Transcribing '$f' ..."
            # run module as autosub.transcribe_accurate; PYTHONPATH=src ensures imports work
            if python -m autosub.transcribe_accurate --input "$f" --model "${MODEL}" --device cpu; then
              echo "Transcription succeeded for: $f"
            else
              echo "Transcription FAILED for: $f"
              FAIL_COUNT=$((FAIL_COUNT+1))
            fi
          done

          if [ "$FAIL_COUNT" -eq "${#FILES[@]}" ]; then
            echo "ERROR: All transcriptions failed ($FAIL_COUNT/${#FILES[@]}). See logs above."
            exit 1
          fi

          echo "Transcription step finished. Successful: $(( ${#FILES[@]} - FAIL_COUNT )), Failed: $FAIL_COUNT"

      - name: Debug: list created subtitles & transcribe.log
        run: |
          echo "==== workspace files ===="
          find . -maxdepth 6 -type f | sed -e 's/^/FILE: /'
          echo "==== subtitle files ===="
          find . -type f \( -iname "*.srt" -o -iname "*.vtt" \) -print || echo "No .srt/.vtt files found"
          echo "==== transcribe.log (if exists) ===="
          if [ -f transcribe.log ]; then
            echo "---- transcribe.log start ----"
            sed -n '1,200p' transcribe.log || true
            echo "---- transcribe.log end ----"
          else
            echo "transcribe.log not found"
          fi

      - name: Upload debug artifacts (subtitles + log)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: transcribe-debug
          path: |
            transcribe.log
            **/*.srt
            **/*.vtt

      - name: Commit generated subtitles (robust)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          mapfile -t SUB_FILES < <(find . -maxdepth 6 -type f \( -iname "*.srt" -o -iname "*.vtt" \) -print)

          if [ ${#SUB_FILES[@]} -eq 0 ]; then
            echo "No subtitle files found to commit."
            exit 0
          fi

          echo "Subtitle files to commit:"
          for f in "${SUB_FILES[@]}"; do
            echo " - $f"
          done

          git add -- "${SUB_FILES[@]}"
          echo "Files staged (git status):"
          git status --short

          if git diff --cached --quiet; then
            echo "No subtitle changes to commit."
            exit 0
          fi

          git commit -m "Auto-generated subtitles [skip ci]" || echo "Nothing to commit"
          git push origin HEAD:${{ github.ref_name }}

      - name: Final artifact upload (subtitles)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: generated-subtitles
          path: |
            **/*.srt
            **/*.vtt
